# Railway — backend NewsFoundry.
# Config actuelle Pad189 : Root Directory = backend/ → voir backend/railway.toml + backend/Dockerfile.
# Alternative : Root Directory vide (racine repo) → dockerfilePath = backend/Dockerfile.monorepo

[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile.monorepo"
watchPatterns = ["backend/**"]
