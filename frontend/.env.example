# Copier en .env ou .env.local (déjà ignorés par git — ne pas les committer).
# Sur Vercel, définir NEXT_PUBLIC_API_URL dans Project → Settings → Environment Variables.
#
# Option A — proxy (recommandé en local, même origine que le front) :
# NEXT_PUBLIC_API_URL=/api-backend
# BACKEND_PROXY_TARGET=http://127.0.0.1:8000
#
# Option B — URL absolue directe (doit correspondre au PORT du backend) :
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
